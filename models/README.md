# Model Weights

The trained model weights are not included in this repository because of file size limitations.

Place the trained .pth file in this folder before running the Streamlit app.

Expected example:

models/resnet50_best.pth
